# Leave empty in local Vite dev so `/api` stays same-origin and mobile devices
# can go through the Vite dev proxy to the backend on your PC.
VITE_API_BASE_URL=
VITE_DEV_API_PROXY_TARGET=http://127.0.0.1:4000
VITE_ENABLE_CSV_IMPORT=false
